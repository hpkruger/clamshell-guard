# AwakeToggle repository instructions

- When app changes are built, completion includes installing the resulting
  `AwakeToggle.app` at `/Applications/AwakeToggle.app`, restarting the installed
  app, and verifying that it is running from that path.
- Verify that the SHA-256 hash of the installed executable matches the freshly
  built executable before reporting the work complete.
